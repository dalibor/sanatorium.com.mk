.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admin_downloads_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_download_path
      %li.active= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_admin_download_path
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      Download
    .inner
      = form_for @download, :url => admin_download_path(@download), :html => { :class => :form, :multipart => true } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
