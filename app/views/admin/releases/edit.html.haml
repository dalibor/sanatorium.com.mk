.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admin_releases_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_release_path
      %li.active= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_admin_release_path
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      Release
    .inner
      = form_for @release, :url => admin_release_path(@release), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
