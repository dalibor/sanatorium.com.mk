%ul.releases
  - @releases.each do |release|
    %li
      %h3
        = link_to release.name, release
        %span= "(#{release.year})"
      = link_to image_tag(release.cover_url(:thumb)), release
      %p= release.label

