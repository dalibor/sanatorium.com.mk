.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", blog.admin_posts_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", blog.new_admin_post_path
      %li.active= link_to "#{t("web-app-theme.edit", :default => "Edit")}", blog.edit_admin_post_path
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      Post
    .inner
      = form_for @post, :url => blog.admin_post_path(@post), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
